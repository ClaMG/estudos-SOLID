export class IUserRegisterUseCase {
    async execute(data) {
        throw new Error("O método execute(data) do register deve ser implementado!");
    }
}

export class IUserDeleteUseCase {
    async execute(data) {
        throw new Error("O método execute(data) do delet deve ser implementado!");
    }
}

export class IUserUpdateUseCase {
    async execute(data) {
        throw new Error("O método execute(data) do update deve ser implementado!");
    }
}

export class IUserGetUseCase {
    async execute() {
        throw new Error("O método execute() do get deve ser implementado!");
    }
}

export class IUserLoginUseCase {
    async execute(data) {
        throw new Error("O método execute() do get deve ser implementado!");
    }
}